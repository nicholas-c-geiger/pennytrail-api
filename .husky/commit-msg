#!/bin/sh
# Cross-platform invocation: use npx.cmd on Windows (Git Bash), otherwise use npx
if [ -n "$WINDIR" ]; then
	npx.cmd --no -- commitlint --edit "$1"
else
	npx --no -- commitlint --edit "$1"
fi
